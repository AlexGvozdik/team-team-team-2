{{#each this}}
  <li class='gallery-items' data-index='{{id}}'>
    <div class='one-card'>
      <div class='moviе-item__img'>
      <img 
        class='js-card-img' 
        src='{{poster_path}}' 
        alt='moviе poster: {{title}}'
      />
    </div>
    <div class='moviе-stats'>
      <h2 class='moviе-stats__title'>
        {{title}}
      </h2>
      <div class='moviе-stats__info'>
        <ul class='moviе-genre'>
    
          <li class='moviе-genre__item'>{{genres_short_list}}</li>
    
        </ul>
        <p class='moviе-year'>
          {{#if release_date}}
          <year class='moviе-year__getFullYear'>{{release_date}}</year>
          {{else}}
          <year class='moviе-year__getFullYear'>{{first_air_date}}</year>
          {{/if}}
        </p>
        <p class='moviе-vote'>
          {{vote_average}}
        </p>
      </div>
    </div>
    <div class='one-card_overlay'>
      <div class='one-card__average'>{{vote_average}}</div>
    <button class='btn_one-card modal-btn-watched js-modal-btn-watched' type='button'>add to watched</button>
    <button class='visually-hidden btn_one-card modal-btn-remove modal-btn-remove-watched js-modal-btn-remove-watched'
      type='button' data-id='{{id}}'>remove from watched</button>
    <button class='btn_one-card modal-btn-queue js-modal-btn-queue' type='button'>add to queue</button>
    <button class='visually-hidden btn_one-card modal-btn-remove modal-btn-remove-queue js-modal-btn-remove-queue'
      type='button'>remove from queue</button>
    </div>
    </div>
    
  </li>
{{/each}}